"use strict";
describe('Array', function () {
    it('should same with javascript', function () {
        const name = ["beni", "via"];
        const age = [19, 20];
        console.info(name);
    });
});
