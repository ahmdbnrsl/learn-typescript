describe('Indexable interface', function () {
    it('should support in Typescript', function () {
        const names = ["viaaa", "via"];
        const namess = {
            "name": "via"
        };
        console.info(namess);
    });
});
export {};
