describe('Intersection type', function () {
    it('should support in Typescript', function () {
        const domain = {
            id: "1",
            name: "via"
        };
    });
});
export {};
