describe('Array', function () {
  it('should same with javascript', function () {
    const name: string[] = ["beni", "via"];
    const age: number[] = [19, 20];
    
    console.info(name);
  });
});